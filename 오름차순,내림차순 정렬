#include <algorithm> // std::sort 등 사용

int main() {
    std::vector<int> arr = {3, 1, 4, 1, 5, 9, 2};

    // 오름차순 정렬
    std::sort(arr.begin(), arr.end());

    // 내림차순 정렬
    std::sort(arr.rbegin(), arr.rend());

    return 0;
}
